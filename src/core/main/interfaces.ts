// This will be used later in our root reducer and selectors
export const NAME = "main";

export type State = {
  currentLocation: any,
  locations: any,
  isFirst: boolean,
  isLoading: boolean,
  status: string,
};
